node_modules
.svelte-kit
build
.vite

# Runtime data (databases, uploaded maps, snapshots, pid/lock files, audit log).
# The data/ dir itself is created at install time, not tracked in the repo.
data/

# Deploy layout (server-side only)
releases/
current
previous

npm-debug.log*

.DS_Store
